-- Generated from prisma/schema.prisma for the timetable proxy assignment module.
CREATE TABLE "StaffSubject" ("staffId" TEXT NOT NULL, "subjectId" TEXT NOT NULL, "isPreferred" BOOLEAN NOT NULL DEFAULT false, PRIMARY KEY ("staffId", "subjectId"), CONSTRAINT "StaffSubject_staffId_fkey" FOREIGN KEY ("staffId") REFERENCES "Staff" ("id") ON DELETE CASCADE ON UPDATE CASCADE, CONSTRAINT "StaffSubject_subjectId_fkey" FOREIGN KEY ("subjectId") REFERENCES "Subject" ("id") ON DELETE CASCADE ON UPDATE CASCADE);
CREATE TABLE "TeacherLeave" ("id" TEXT NOT NULL PRIMARY KEY, "teacherId" TEXT NOT NULL, "date" TEXT NOT NULL, "status" TEXT NOT NULL DEFAULT 'APPROVED', "reason" TEXT, "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "TeacherLeave_teacherId_fkey" FOREIGN KEY ("teacherId") REFERENCES "Staff" ("id") ON DELETE CASCADE ON UPDATE CASCADE);
CREATE TABLE "ProxyAssignment" ("id" TEXT NOT NULL PRIMARY KEY, "timetableSlotId" TEXT NOT NULL, "absentTeacherId" TEXT NOT NULL, "proxyTeacherId" TEXT, "date" TEXT NOT NULL, "status" TEXT NOT NULL DEFAULT 'PENDING', "recommendations" TEXT NOT NULL DEFAULT '[]', "selectedByUserId" TEXT, "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" DATETIME NOT NULL, CONSTRAINT "ProxyAssignment_timetableSlotId_fkey" FOREIGN KEY ("timetableSlotId") REFERENCES "TimetableSlot" ("id") ON DELETE CASCADE ON UPDATE CASCADE, CONSTRAINT "ProxyAssignment_proxyTeacherId_fkey" FOREIGN KEY ("proxyTeacherId") REFERENCES "Staff" ("id") ON DELETE SET NULL ON UPDATE CASCADE);
CREATE TABLE "TeacherWorkload" ("id" TEXT NOT NULL PRIMARY KEY, "teacherId" TEXT NOT NULL, "date" TEXT NOT NULL, "lectureCount" INTEGER NOT NULL DEFAULT 0, "proxyCount" INTEGER NOT NULL DEFAULT 0, "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" DATETIME NOT NULL, CONSTRAINT "TeacherWorkload_teacherId_fkey" FOREIGN KEY ("teacherId") REFERENCES "Staff" ("id") ON DELETE CASCADE ON UPDATE CASCADE);
CREATE TABLE "RoomReservation" ("id" TEXT NOT NULL PRIMARY KEY, "roomId" TEXT NOT NULL, "date" TEXT NOT NULL, "day" TEXT NOT NULL, "period" INTEGER NOT NULL, "status" TEXT NOT NULL DEFAULT 'ACTIVE', "timetableSlotId" TEXT, "reason" TEXT, "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "RoomReservation_roomId_fkey" FOREIGN KEY ("roomId") REFERENCES "Room" ("id") ON DELETE CASCADE ON UPDATE CASCADE);
CREATE TABLE "SchedulingConstraint" ("id" TEXT NOT NULL PRIMARY KEY, "key" TEXT NOT NULL, "value" TEXT NOT NULL, "isActive" BOOLEAN NOT NULL DEFAULT true, "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" DATETIME NOT NULL);
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Staff" ("id" TEXT NOT NULL PRIMARY KEY, "name" TEXT NOT NULL, "email" TEXT NOT NULL, "department" TEXT NOT NULL, "maxPeriodsPerDay" INTEGER NOT NULL DEFAULT 4, "maxPeriodsPerWeek" INTEGER NOT NULL DEFAULT 24, "maxProxiesPerDay" INTEGER NOT NULL DEFAULT 2, "isActive" BOOLEAN NOT NULL DEFAULT true, "avatar" TEXT, "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" DATETIME NOT NULL);
INSERT INTO "new_Staff" ("avatar","createdAt","department","email","id","maxPeriodsPerDay","name","updatedAt") SELECT "avatar","createdAt","department","email","id","maxPeriodsPerDay","name","updatedAt" FROM "Staff";
DROP TABLE "Staff"; ALTER TABLE "new_Staff" RENAME TO "Staff"; CREATE UNIQUE INDEX "Staff_email_key" ON "Staff"("email");
CREATE TABLE "new_Room" ("id" TEXT NOT NULL PRIMARY KEY, "roomNumber" TEXT NOT NULL, "building" TEXT NOT NULL, "capacity" INTEGER NOT NULL, "type" TEXT NOT NULL DEFAULT 'LECTURE', "isAvailable" BOOLEAN NOT NULL DEFAULT true, "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" DATETIME NOT NULL);
INSERT INTO "new_Room" ("building","capacity","createdAt","id","roomNumber","type","updatedAt") SELECT "building","capacity","createdAt","id","roomNumber","type","updatedAt" FROM "Room";
DROP TABLE "Room"; ALTER TABLE "new_Room" RENAME TO "Room"; CREATE UNIQUE INDEX "Room_roomNumber_key" ON "Room"("roomNumber");
CREATE TABLE "new_Subject" ("id" TEXT NOT NULL PRIMARY KEY, "code" TEXT NOT NULL, "name" TEXT NOT NULL, "weeklyPeriods" INTEGER NOT NULL DEFAULT 3, "requiresLab" BOOLEAN NOT NULL DEFAULT false, "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" DATETIME NOT NULL);
INSERT INTO "new_Subject" ("code","createdAt","id","name","updatedAt","weeklyPeriods") SELECT "code","createdAt","id","name","updatedAt","weeklyPeriods" FROM "Subject";
DROP TABLE "Subject"; ALTER TABLE "new_Subject" RENAME TO "Subject"; CREATE UNIQUE INDEX "Subject_code_key" ON "Subject"("code");
PRAGMA foreign_keys=ON;
CREATE INDEX "TeacherLeave_date_status_idx" ON "TeacherLeave"("date","status");
CREATE UNIQUE INDEX "TeacherLeave_teacherId_date_key" ON "TeacherLeave"("teacherId","date");
CREATE INDEX "ProxyAssignment_date_proxyTeacherId_idx" ON "ProxyAssignment"("date","proxyTeacherId");
CREATE UNIQUE INDEX "ProxyAssignment_timetableSlotId_date_key" ON "ProxyAssignment"("timetableSlotId","date");
CREATE UNIQUE INDEX "TeacherWorkload_teacherId_date_key" ON "TeacherWorkload"("teacherId","date");
CREATE INDEX "RoomReservation_date_day_period_roomId_idx" ON "RoomReservation"("date","day","period","roomId");
CREATE UNIQUE INDEX "SchedulingConstraint_key_key" ON "SchedulingConstraint"("key");
CREATE INDEX "TimetableSlot_day_period_teacherId_idx" ON "TimetableSlot"("day","period","teacherId");
CREATE INDEX "TimetableSlot_day_period_roomId_idx" ON "TimetableSlot"("day","period","roomId");
